module(
    name = "quant_competitions",
    version = "1.0.0",
)

# Only require rules_shell for the current sh_test orchestration strategy
bazel_dep(name = "rules_shell", version = "0.6.1")
bazel_dep(name = "platforms", version = "0.0.10")
