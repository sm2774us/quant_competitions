workspace(name = "two_sigma_challenge")

load("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")

# Add rules for different languages if needed, 
# but for a simple unified entry point we will use sh_rules to call local tools.
