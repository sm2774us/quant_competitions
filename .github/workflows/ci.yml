name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  bazel-build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      # --- Toolchain Setup ---
      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install Poetry
        run: curl -sSL https://install.python-poetry.org | python3 -

      - name: Install C++ Tools (GCC 14, CMake, Ninja)
        run: |
          sudo apt-get update
          sudo apt-get install -y g++-14 cmake ninja-build libcurl4-openssl-dev

      - name: Set up Rust 1.93.1
        run: |
          rustup toolchain install stable --profile minimal
          rustup default stable

      # --- Bazel Setup & Execution ---
      - name: Install Bazelisk
        run: |
          npm install -g @bazel/bazelisk

      - name: Run All Tests via Bazel
        run: |
          # Use Bazel to orchestrate the build/test of all targets defined in BUILD.bazel
          bazel test //... --test_output=errors
